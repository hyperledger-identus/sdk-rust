# Rust build artifacts
/target
/tests/wasm-did-browser/target
/tools/uniffi-bindgen/target
__pycache__/
/fuzz/artifacts
/fuzz/target

# Nix
/result
/result-*
/tmp/

# Pi creates project package state here when bootstrap is bypassed. The
# canonical bootstrap path is an ignored symlink to an external exact cache.
/.pi/npm

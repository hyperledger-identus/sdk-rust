## 1. Planning and authority

- [x] 1.1 Record current GitHub scheduling semantics, branch/ruleset state,
      retention, alternatives, protected-setting authority and exact base.
- [x] 1.2 Pass strict OpenSpec, research and constraint readiness and persist a
      planning-only pre-implementation receipt.

## 2. Repository contract

- [ ] 2.1 Add ADR 0120 and update branch, factory, architecture, constraint and
      capability truth from pending to the selected native mechanism.
- [ ] 2.2 Update machine policy, support-policy validator/tests and target-plan
      semantics for active weekly/manual slow evidence.
- [ ] 2.3 Bound the slow workflow and add immutable always-run metadata evidence
      without changing required fast CI or SDK runtime code.
- [ ] 2.4 Add an offline scheduling contract and network-explicit scheduled-run
      freshness command with positive and fail-closed tests.

## 3. Review and first PR

- [ ] 3.1 Run focused workflow/policy/factory/Nix checks and complete a distinct
      architecture/security/operations review.
- [ ] 3.2 Archive the change, push signed DCO commits and merge an issue-linked
      activation PR into protected `develop` without closing #276.

## 4. Live activation and receipts

- [ ] 4.1 Rebind the reserved ruleset to `refs/heads/main`, make protected
      `develop` default, and verify both live states without populating `main`.
- [ ] 4.2 Dispatch the merged slow workflow at exact `develop`, observe every
      job, and record duration/conclusion/artifact evidence.
- [ ] 4.3 Create the read-only weekly supervisor heartbeat and merge a dated
      live settings/canary receipt.
- [ ] 4.4 After the first natural schedule, verify freshness and exact SHA,
      merge the scheduled-run receipt, close #276 and retain rollback details.

## 1. Planning contract

- [x] 1.1 Record the exact base, live Pi v3 research, vault guidance,
      candidate dispositions, privacy boundary and explicit limitations.
- [x] 1.2 Pass strict OpenSpec, research and constraint readiness before any
      supervisor, handoff or metrics implementation edit.

## 2. Supervisor and worker protocol

- [ ] 2.1 Add closed invocation, heartbeat and terminal handoff schemas plus
      private exact-identity preparation and validation.
- [ ] 2.2 Launch Pi through bootstrap with a role-bounded tool list, external
      heartbeat, hard deadline, process cleanup and distinct handoff status.
- [ ] 2.3 Harvest exact Pi v3 sessions/turns/tools/token buckets without
      retaining content, identifiers, cost, provider/model or raw output.

## 3. Metrics v2 and evidence

- [ ] 3.1 Add closed metrics v2 validation, template, storage, rendering and
      versioned publication while retaining v1 compatibility.
- [ ] 3.2 Add known-good and known-bad tests for stale identity, malformed or
      oversized records, paths, timeout, privacy, usage deduplication and
      publication-safe aggregates; update factory documentation.
- [ ] 3.3 Run focused/end-to-end gates and one bounded live Pi canary, record
      exact metrics, perform a distinct local review, archive the change and
      prepare the signed issue-linked PR.

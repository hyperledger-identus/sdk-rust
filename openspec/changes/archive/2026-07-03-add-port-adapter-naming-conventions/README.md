# add-port-adapter-naming-conventions

Port/adapter naming convention (ports: no suffix; adapters: *Adapter) with an inert `#[identus::port]` attribute (compile-time port-name check) and a test-time syn-based conformance guard, plus the conformance-crate module structure.

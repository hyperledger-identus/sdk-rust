[package]
name = "identus-crypto"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "JubJub embedded curve and Schnorr signature types for the Identus SDK"

[dependencies]
midnight-transient-crypto = { workspace = true }
# Re-export rand so callers of sign() can satisfy the CryptoRng bound
rand = { workspace = true }

[dev-dependencies]
# Deterministic RNG for reproducible test vectors
rand_chacha = { workspace = true }

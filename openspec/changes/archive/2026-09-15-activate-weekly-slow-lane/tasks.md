## 1. Planning and authority

- [x] 1.1 Record current GitHub scheduling semantics, branch/ruleset state,
      retention, alternatives, protected-setting authority and exact base.
- [x] 1.2 Pass strict OpenSpec, research and constraint readiness and persist a
      planning-only pre-implementation receipt.

## 2. Repository contract

- [x] 2.1 Add ADR 0120 and update branch, factory, architecture, constraint and
      capability truth from pending to the selected native mechanism.
- [x] 2.2 Update machine policy, support-policy validator/tests and target-plan
      semantics for active weekly/manual slow evidence.
- [x] 2.3 Bound the slow workflow and add immutable always-run metadata evidence
      without changing required fast CI or SDK runtime code.
- [x] 2.4 Add an offline scheduling contract and network-explicit scheduled-run
      freshness command with positive and fail-closed tests.

## 3. Review and activation PR

- [x] 3.1 Run focused workflow/policy/factory/Nix checks and complete a distinct
      architecture/security/operations review.
- [x] 3.2 Prepare the guarded archive for an issue-linked activation PR targeting
      protected `develop` without closing #276.

## Post-merge issue acceptance

The following live operations remain owned by issue #276 rather than this
pre-merge OpenSpec archive: rebind the reserved ruleset to explicit `main`, make
protected `develop` default, run and record the manual canary, create the
read-only supervisor heartbeat, and close the issue only after the first natural
scheduled run has a reviewed receipt.

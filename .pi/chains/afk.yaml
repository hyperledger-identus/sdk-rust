steps:
  - prompt: Run `scripts/factory research-ready $ARGUMENTS`; stop before implementation if it fails
  - prompt: Run `scripts/factory constraints-ready $ARGUMENTS`; stop before implementation if it fails
  - type: callChain
    name: apply-change
    argument: $ARGUMENTS
  - prompt: Use the `openspec-verify-change` skill to verify $ARGUMENTS
  - prompt: Run `scripts/factory ready $ARGUMENTS` and report the exact result

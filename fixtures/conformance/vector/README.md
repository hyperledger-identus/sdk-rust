# Vector Fixtures

Use this directory for deterministic vectors such as key derivation, DID
parsing, credential verification, status-list decoding, and canonical
serialization.

Expected future layout:

```text
<spec-id>/
  vectors/*.json
  README.md
```

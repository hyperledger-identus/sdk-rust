## 1. Program Contract

- [x] 1.1 Create GitHub issue #20 before implementation and record the source
  limitation, repository boundaries, immutable inspected revisions and
  existing issue reconciliation.
- [x] 1.2 Define the backlog schema, evidence classifications, stop/go rules
  and semantic preflight with no unresolved blocker.

## 2. Canonical Backlog

- [x] 2.1 Add all thirty SDK-owned rows with preserved outcomes and enriched
  delivery, issue, ownership, predecessor, normative and source fields.
- [x] 2.2 Add the repository/source extraction matrix and explicit downstream
  retention boundaries.
- [x] 2.3 Update the blueprint and roadmap to use the canonical backlog and its
  ordered component slices.

## 3. Executable Checks

- [x] 3.1 Add deterministic schema and semantic validation for the canonical
  CSV, including a focused positive and negative test surface.
- [x] 3.2 Integrate the validation into the repository structural gate without
  adding a donor or consumer build dependency.

## 4. Verification and Finalization

- [x] 4.1 Pass strict OpenSpec and factory checks plus all documentation and
  focused backlog-validator tests.
- [x] 4.2 Confirm each donor/consumer worktree remained unedited and record the
  exact references and known local dirt as read-only boundary evidence.
- [x] 4.3 Synchronize the current capability spec, archive the change and
  produce the factory readiness receipt.

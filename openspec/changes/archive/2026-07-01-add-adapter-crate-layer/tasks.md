## 1. Ring-layout spec delta + identus-adapters-entropy skeleton

- [x] 1.1 Add `identus-adapters-entropy = { path = "crates/adapters-entropy" }` and `ring = "0.17"` to root `Cargo.toml` `[workspace.dependencies]` (entropy is the sole workspace consumer of `ring`; `add-crypto-capability` no longer declares `ring`)
- [x] 1.2 Create `crates/adapters-entropy/Cargo.toml`: `[package]` (`name = "identus-adapters-entropy"`, `version`/`edition`/`rust-version`/`license` `.workspace = true`); `[dependencies]` `identus-core.workspace = true`, `identus-crypto.workspace = true`; `ring = { workspace = true, optional = true }`; `[features] ring = ["dep:ring"], default = []`; `[lints] workspace = true`
- [x] 1.3 Create `crates/adapters-entropy/src/lib.rs`: module doc-comment (entropy port-family adapter crate; `ring`-backed `SecureRandom` adapter lands here via `add-crypto-capability`; `getrandom`/wasm adapter deferred to a future TS/wasm binding change) + `pub const COMPONENT: Component = Component { name: "identus-adapters-entropy", summary: "Entropy-port adapters (SecureRandom) for the Identus Rust SDK." }`
- [x] 1.4 `crates/conformance/src/lib.rs` `LAYER_RULES`: add `"identus-adapters-entropy"` to the `Layer::OuterBoundary` `members` array
- [x] 1.5 `crates/conformance/src/lib.rs` guard: replace the hard-coded `13` in `every_crate_appears_in_exactly_one_layer` (`assert_eq!(total, 13, …)`) and `manifests_conform_to_layer_rules` (`assert_eq!(manifest_paths.len(), 13, …)`) with the derived total `LAYER_RULES.iter().map(|r| r.members.len()).sum::<usize>()` (and the manifest-count assertion SHALL compare to the same derived total); replace the hand-maintained `OUTER_CRATES` const used by the production-must-not-depend-on-outer check with a `LAYER_RULES`-derived outer/verification set (`Layer::OuterBoundary` ∪ `Layer::Verification`); update the stale "13 crates" comment to reflect that the count is derived from `LAYER_RULES`
- [x] 1.6 Verify `cargo test -p identus-conformance` passes (new member admitted; production crates still reject outer edges)

## 2. Conformance + hardening

- [x] 2.1 `cargo build -p identus-adapters-entropy` (default features) compiles the stub with no `ring` pull-in
- [x] 2.2 `cargo build -p identus-adapters-entropy --features ring` resolves the `ring` workspace dep against the empty stub (adapter content is filled by `add-crypto-capability`)
- [x] 2.3 `cargo fmt --check`, `cargo clippy -- -D warnings`, `cargo test --workspace` pass
- [x] 2.4 `nix flake check` green (the new stub + conformance guard under the existing `rust-test` / `rust-audit` / `cargo-deny` checks)

## 3. OpenSpec artifacts

- [x] 3.1 Finalize `proposal.md`, `design.md`, `specs/crate-ring-layout/spec.md`, `tasks.md`. The spec delta is no longer ADDED-only: it RENAMES `### Requirement: Twelve crate stubs at minimal code depth` → `### Requirement: Crate stubs at minimal code depth` (non-literal body: founding runtime stubs + `identus-adapters-entropy` + by-reference admission of future adapter-family stubs), MODIFIES `### Requirement: Crate stubs at minimal code depth` (full new body, post-rename header) and `### Requirement: Workspace dependency map` (`LAYER_RULES`-derived count, no literal), and ADDS the four adapter-family requirements. `proposal.md`'s prior "no MODIFIED / no collision" claims are corrected to state the intentional collision with `add-domain-newtype-macro` and the ordered-archive resolution; `design.md` gains Decision 6 (non-literal counts; guard-relevance split) and the archive-order reconciliation note.
- [x] 3.2 `openspec validate add-adapter-crate-layer --strict` and resolve any findings (the RENAMED+MODIFIED pair must satisfy: MODIFIED references the NEW header `Crate stubs at minimal code depth`, not the FROM header)
- [x] 3.3 Archive order: **archive `add-adapter-crate-layer` first**, then `add-domain-newtype-macro`. This change's RENAMED + MODIFIED blocks are self-contained and carry no `identus-derive` content. When `add-domain-newtype-macro` archives next, its MODIFIED blocks for `Crate stubs at minimal code depth` and `Workspace dependency map` SHALL be retargeted to the renamed/derived headers and carry the full combined content (founding runtime stubs + `identus-adapters-entropy` + by-reference admission + `identus-derive` exclusion; derived workspace-dep set including both `identus-adapters-entropy` and `identus-derive`). Note: until `add-adapter-crate-layer` archives, `add-domain-newtype-macro`'s retargeted MODIFIED header does not exist in the main spec, so `add-domain-newtype-macro` is archive-blocked-by this change and not standalone-`openspec validate`-able in the interim.